import { EMPTY_STR } from '@constants';
const initials = {
  listenKey: EMPTY_STR,
  selectedStream: {
    type: EMPTY_STR,
    codes: []
  },
  selectedUserStream: EMPTY_STR,
  stream: [],
  path: EMPTY_STR
};
export default initials;
